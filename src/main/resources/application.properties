spring.application.name=everyones-coupon

# spring.datasource.url=
# spring.datasource.username=
# spring.datasource.password=

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

#app.image.upload-dir=uploads
# use separated base-host and base-path instead of base-url
app.image.base-host=http://localhost:8080
#app.image.base-path=/uploads
#env var copy sheet
#APP_IMAGE_BASE_HOST=https://everyones-coupon-webapp.azurewebsites.net
#APP_IMAGE_UPLOAD_DIR=/home/site/wwwroot/uploads

# not need
#APP_IMAGE_BASE_PATH=/uploads
